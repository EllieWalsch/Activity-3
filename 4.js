// primitive data types

// String: previous examples

// Number: previous examples

// Booleans : either true or false
const isAdult = true;
const canDrink = false;

// Null: empty value, "nothing value"
const nothing = null;

// Undefined: no value assigned - shound never be used. Declared a space in memory but nothing is attached to it
let name;

console.log(isAdult, canDrink, nothing);

console.log(whatever);

// Reference Error Not Defined is a reference error code - no declaration, there is a mistake
